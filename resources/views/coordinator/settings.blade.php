@extends('coordinator.layout')
@section('title','Coordinator Settings')
@section('content')<section class="settings-page"><h2>Settings</h2>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="settings-grid"><div class="setting-card"><h3>Profile Information</h3><form method="POST" action="{{ route('coordinator.settings.update') }}">@csrf<input name="full_name" value="{{ $user->full_name }}" placeholder="Full Name" required><input type="email" name="email" value="{{ $user->email }}" placeholder="Email Address" required><input name="business_name" value="{{ $user->business_name }}" placeholder="Business Name"><button>Save Changes</button></form></div><div class="setting-card"><h3>Notifications</h3><label><input type="checkbox" checked> Booking Alerts</label><label><input type="checkbox" checked> Messages</label><label><input type="checkbox"> Promotions</label><button type="button">Save Preferences</button></div></div></section>@endsection
